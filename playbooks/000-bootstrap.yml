---
- hosts: all
  roles:
    - role: apt
      tags: apt

    - role: system
      tags: system

    - role: oh_my_zsh
      tags: oh_my_zsh

    - role: sshd
      tags: sshd

    - when: ansible_hostname in bind9_zones | default([]) | json_query('[*].master')
      role: bind9
      tags: bind9
